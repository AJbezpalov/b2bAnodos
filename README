# Русскоязычное руководство по использованию git http://githowto.com/

# Настройка git
$git config --global user.name "Your Name"
$git config --global user.email "your_email@whatever.com"

# Что-то связанное с окончаниями строк
$git config --global core.autocrlf input
$git config --global core.safecrlf true

# Создаст папку b2bAnodos
$mkdir ~/b2bAnodos

# Зайдем в папку
$cd ~/b2bAnodos

# Установим необходимые файлы git
# инициализируем пустой репозиторий /Users/<user_name>/b2bAnodos/.git/
$git init

# Создаем файл "README" в папке b2bAnodos
$touch README

# Добавляем файл в репозиторий (подключаем контроль версий)
$git add README

# Фиксируем изменения
$git commit -m "Added README"

# Применяем на github
$git remote add origin https://github.com/AJbezpalov/b2bAnodos.git
$git push origin master

#Проверить статус репозитория -- внесенные изменения
$git status
